---
interface Props {
  headline?: string;
  subtext?: string;
  primaryLabel?: string;
  primaryHref?: string;
  secondaryLabel?: string;
  secondaryHref?: string;
}

const {
  headline = "We build with intention.",
  subtext = "Volition Labs is a space for builders to come together and build great products with intention. Commitspace is our first tool designed for structured group focus sessions.",
  primaryLabel = "See Us In Action",
  primaryHref = "#commitspace",
  secondaryLabel = "Contact us",
  secondaryHref = "#work-with-us",
} = Astro.props;
---

<section class="hero">
  <div class="container">
    <p class="hero-kicker">Work With Us</p>
    <h1 class="hero-headline">{headline}</h1>
    <div class="hero-copy">
      <p class="hero-sub">{subtext}</p>
    </div>
    <div class="hero-actions">
      <a href={primaryHref} class="btn-primary">{primaryLabel}</a>
      <a href={secondaryHref} class="btn-secondary">{secondaryLabel}</a>
    </div>
  </div>
</section>

<style>
  .hero {
    padding: 88px 0 72px;
    border-bottom: 1px solid var(--border);
  }

  .hero-kicker {
    font-size: 0.78rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--blue-green);
    margin-bottom: 14px;
  }

  .hero-headline {
    max-width: 740px;
    font-size: clamp(2rem, 4.1vw, 3.4rem);
    font-weight: 600;
    line-height: 1.14;
    letter-spacing: -0.025em;
    margin-bottom: 18px;
  }

  .hero-sub {
    font-size: 1rem;
    color: var(--text-muted);
    max-width: 620px;
    line-height: 1.75;
    margin: 0;
  }

  .hero-copy {
    margin-bottom: 30px;
  }

  .hero-copy :global(p) {
    font-size: 1rem;
    color: var(--text-muted);
    max-width: 620px;
    line-height: 1.75;
    margin: 0;
  }

  .hero-copy :global(p + p) {
    margin-top: 0.9em;
  }

  .hero-actions {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 12px;
  }
</style>
